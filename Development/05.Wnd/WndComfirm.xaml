<Window x:Class="Development.WndComfirm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Development"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="WndComfirm" Height="220" Width="900">
    <Grid MouseDown="Window_MouseDown" Margin="10">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="2" Color="Black"/>
        </Grid.Effect>

        <Border CornerRadius="8" Background="White" BorderThickness="1" BorderBrush="#DDDDDD">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2.5*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#2D2D2D" CornerRadius="7,7,0,0">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="tbxMessage" 
                                   Text="Message :" 
                                   FontSize="18" 
                                   Foreground="White" 
                                   FontFamily="Segoe UI Semibold"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   Margin="20,0"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1" Background="#EEEEEE">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" x:Name="btOk" 
                            Content="Confirm" 
                            Width="140" Height="45"
                            Cursor="Hand"
                            BorderThickness="0"
                            Foreground="White"
                            FontWeight="SemiBold"
                            FontSize="16">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#4CAF50"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45a049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="1" x:Name="btCaner" 
                            Content="Cancel" 
                            Width="140" Height="45"
                            Cursor="Hand"
                            BorderThickness="1"
                            BorderBrush="#DDDDDD"
                            Foreground="#555555"
                            FontWeight="SemiBold"
                            FontSize="16">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="OrangeRed"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>