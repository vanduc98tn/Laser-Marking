<Window x:Class="Development.WndMessenger"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Development"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="WndMessenger" Height="220" Width="900">
    <Grid MouseDown="Window_MouseDown" Margin="10">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.2" ShadowDepth="2" Color="Black"/>
        </Grid.Effect>

        <Border CornerRadius="8" Background="White" BorderThickness="1" BorderBrush="#DDDDDD">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2.5*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#2D2D2D" CornerRadius="7,7,0,0">
                    <Grid>
                        <TextBlock x:Name="tbxMessage" 
                                   Text="Message :" 
                                   FontSize="18" 
                                   Foreground="White" 
                                   FontFamily="Segoe UI Semibold"
                                   TextAlignment="Center" 
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap" 
                                   Margin="30,10"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" Background="#EEEEEE">
                    <Button x:Name="btOk" 
                            Content="Confirm" 
                            Width="150" Height="42"
                            Cursor="Hand"
                            BorderThickness="0"
                            Foreground="White"
                            FontWeight="SemiBold"
                            FontSize="15">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2196F3"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="21">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>